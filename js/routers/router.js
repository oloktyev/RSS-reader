var app = app || {};
ReaderRouter = Backbone.Router.extend({
    /* define the route and function maps for this router */
    routes: {
	}
});

/* Now that we have a router setup, we need to instantiate it */

app.ReaderRouter = new ReaderRouter();